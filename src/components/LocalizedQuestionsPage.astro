---
import Icon from './Icon.astro';
import { questionAlternates } from '../data/localizedQuestions';

interface QuestionItem {
  q: string;
  answer: string;
  href: string;
  guide: string;
  related?: { label: string; href: string }[];
}

interface QuestionGroup {
  title: string;
  questions: QuestionItem[];
}

interface PageData {
  lang: string;
  path: string;
  label: string;
  eyebrow: string;
  heading: string;
  intro: string;
  whyTitle: string;
  whyCopy: string;
  steps: readonly (readonly [string, string])[];
  readAnswer: string;
  submitTitle: string;
  submitCopy: string;
  submitButton: string;
  questionPlaceholder: string;
  emailLabel: string;
  groups: readonly QuestionGroup[];
}

const { page } = Astro.props as { page: PageData };
---

<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-16">
  <div class="flex flex-wrap gap-2">
    {questionAlternates.filter((alternate) => alternate.hreflang !== 'x-default').map((alternate) => (
      <a
        href={alternate.href}
        class:list={[
          'ds-chip',
          alternate.href === page.path ? '!border-accent !bg-accent !text-white' : 'hover:border-accent/50 hover:text-accent',
        ]}
      >
        {alternate.hreflang === 'en' ? 'English' : alternate.hreflang === 'ko' ? '한국어' : '日本語'}
      </a>
    ))}
  </div>

  <p class="ds-eyebrow mt-8">
    {page.eyebrow}
  </p>
  <div class="mt-3 grid gap-6 lg:grid-cols-[0.9fr_1.1fr] lg:items-end">
    <div>
      <h1 class="text-4xl font-bold leading-tight sm:text-5xl">
        {page.heading}
      </h1>
    </div>
    <p class="text-soft text-lg leading-relaxed">
      {page.intro}
    </p>
  </div>
</section>

<section class="mx-auto max-w-6xl px-4 sm:px-6">
  <div class="ds-panel p-4 sm:p-6">
    <div class="grid gap-6 lg:grid-cols-[0.8fr_1.2fr] lg:items-start">
      <div>
        <h2 class="text-3xl font-bold">{page.whyTitle}</h2>
        <p class="mt-3 text-soft leading-relaxed">{page.whyCopy}</p>
      </div>
      <div class="grid gap-3 sm:grid-cols-3">
        {page.steps.map(([title, copy]) => (
          <div class="ds-card p-5">
            <p class="ds-tag">{title}</p>
            <p class="text-soft text-sm mt-2 leading-relaxed">{copy}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<section class="mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-16">
  <div class="space-y-10">
    {page.groups.map((group) => (
      <section aria-labelledby={group.title.toLowerCase().replaceAll(' ', '-')}>
        <h2 id={group.title.toLowerCase().replaceAll(' ', '-')} class="text-2xl font-bold">{group.title}</h2>
        <div class="mt-4 grid gap-4 lg:grid-cols-3">
          {group.questions.map((item) => (
            <a href={item.href} class="ds-card-link group p-5">
              <p class="ds-tag text-accent">{item.guide}</p>
              <h3 class="mt-2 text-xl font-bold leading-snug group-hover:text-accent">{item.q}</h3>
              <p class="mt-2 text-sm leading-relaxed text-soft">{item.answer}</p>
              {item.related && item.related.length > 0 && (
                <div class="mt-4 border-t border-line pt-3">
                  <p class="ds-tag">Also answered by</p>
                  <div class="mt-2 flex flex-wrap gap-2">
                    {item.related.map((related) => (
                      <span class="ds-chip">
                        {related.label}
                      </span>
                    ))}
                  </div>
                </div>
              )}
              <p class="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-accent">
                {page.readAnswer}
                <Icon name="arrow" class="h-3.5 w-3.5" />
              </p>
            </a>
          ))}
        </div>
      </section>
    ))}
  </div>
</section>

<section class="mx-auto max-w-6xl px-4 sm:px-6">
  <div class="ds-card p-6 sm:p-8">
    <div class="grid gap-6 lg:grid-cols-[0.8fr_1.2fr] lg:items-start">
      <div>
        <p class="ds-eyebrow">{page.eyebrow}</p>
        <h2 class="mt-2 text-3xl font-bold">{page.submitTitle}</h2>
        <p class="mt-3 text-soft leading-relaxed">{page.submitCopy}</p>
      </div>
      <form action="mailto:hello@chinatravelmadeeasy.com" method="post" enctype="text/plain" class="grid gap-3">
        <label class="grid gap-1 text-sm font-semibold text-soft">
          {page.submitTitle}
          <textarea name="question" rows="5" class="ds-input font-body" placeholder={page.questionPlaceholder}></textarea>
        </label>
        <label class="grid gap-1 text-sm font-semibold text-soft">
          {page.emailLabel}
          <input name="email" type="email" class="ds-input font-body" placeholder="you@example.com" />
        </label>
        <button type="submit" class="ds-button ds-button-primary justify-self-start">
          {page.submitButton}
        </button>
      </form>
    </div>
  </div>
</section>
